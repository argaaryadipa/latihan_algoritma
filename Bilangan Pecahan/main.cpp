#include<iostream>

using namespace std;

int main() {
    int a,b;
    cin >> a >> b;

    if (a / b == true)
    {
        cout>> a / b;
    } else {
        cout>>"Tidak";
    }
    
}

